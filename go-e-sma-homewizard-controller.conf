# go-e-sma-homewizard-controller Configuration File
#
# This file should be placed in one of these locations:
#   - /etc/go-e-sma-homewizard-controller/go-e-sma-homewizard-controller.conf (system-wide)
#   - $HOME/.config/go-e-sma-homewizard-controller/go-e-sma-homewizard-controller.conf (user-specific)
#   - Same directory as the script (local config)
#
# Format: KEY=VALUE (no spaces around '=' recommended)
# Lines starting with # are comments and are ignored.
# Empty lines are ignored.
#
# The script searches for the config file in the order listed above and uses the first one found.
# Environment variables can override these settings:
#   - CHARGER_IP
#   - P1_IP
#   - MAX_POWER_LIMIT_WATTS
#   - SAFETY_MARGIN_WATTS
#   - SMA_LOG_FILE
#   - P1_CSV_FILE
#   - PV_PHASE_MODE

# IP address of your go-e-charger
# Example: 192.168.1.100
CHARGER_IP=#.#.#.#

# IP address of your HomeWizard P1 Meter
# Example: 192.168.1.101
P1_IP=#.#.#.#

# Maximum power limit in Watts
# This prevents exceeding the power threshold (especially for Austrian >10kW tariff)
# Default: 10000 (10 kW)
MAX_POWER_LIMIT_WATTS=10000

# Safety margin in Watts
# Buffer to avoid short-term power fluctuations causing issues
# Default: 300
SAFETY_MARGIN_WATTS=300

# Phase mode setting for PV charging (psm parameter on go-eCharger)
# 1 = Force 1-phase charging (prevents unwanted grid import on small PV setups)
# 0 = Automatic phase switching / 3-phase allowed
# Default: 1
PV_PHASE_MODE=1

# Path to the SMA inverter log file used for PV power detection
# Use a directory accessible to the service user, not necessarily the root home directory.
# Default: /var/lib/sma-bluetooth/tmp/sma-update.log
SMA_LOG_FILE=/var/lib/sma-bluetooth/tmp/sma-update.log

# Path to P1 Output file for web app usage
# Use a directory accessible to the service user, not necessarily the root home directory.
# Default: disabled (no P1 output file)
# requires security configuration for writing into this path on linux. (see INSTALL.md for details)
# P1_CSV_FILE=/var/www/html/p1/p1-data.csv
